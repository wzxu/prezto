#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Source fzf autocompletion
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# Customize to your needs...
alias up='pacaur -Syu'
alias zap='pacaur -Rsc $(pacaur -Qdtq)'
alias clean='yes | pacaur -Scc'
alias s='subl3'
alias ss='subl3 .'
alias z='s ~/.zshrc'
alias l='ls'
alias l1='ls -1A'
alias ltr='ls -ltr'
alias cls='clear'
alias -g C='| wc -l'
alias -g H='| head'
alias -g L="| less"
alias -g N="| /dev/null"
alias -g S='| sort'
alias -g G='| grep'
